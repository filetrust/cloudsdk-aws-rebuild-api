'use strict';

const app = require('../../app.js');
const chai = require('chai');
const expect = chai.expect;
var event, context;

describe('Rebuild Url', function () {
    it('should return ok', async () => {
        expect(true).toBe(true);
    });
});